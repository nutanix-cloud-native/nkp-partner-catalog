---
title: "NVIDIA GPU Operator"
description: "The NVIDIA GPU Operator manages NVIDIA GPU resources in a Kubernetes cluster and automates tasks related to bootstrapping GPU nodes."
sidebar_label: "NVIDIA GPU Operator"
unlisted: true
---

import AppDetailPage from '@site/src/components/AppDetailPage';
import data from './nvidia-gpu-operator.json';

<AppDetailPage data={data} />
